<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Game Zone</title>
    <style>
        /* CSS để trang web trông chuyên nghiệp hơn */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #1a1a2e;
            color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
            margin: 0;
            padding: 20px;
        }

        header {
            text-align: center;
            margin-bottom: 20px;
        }

        h1 { color: #e94560; }

        #game-container {
            position: relative;
            border: 4px solid #16213e;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 0 20px rgba(0,0,0,0.5);
        }

        canvas {
            background-color: #0f3460;
            display: block;
        }

        .stats {
            margin-top: 15px;
            font-size: 1.2rem;
            font-weight: bold;
        }

        .instructions {
            margin-top: 10px;
            color: #95a5a6;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>

    <header>
        <h1>🎮 MY GAME ZONE</h1>
        <p>Chào mừng bạn đến với trang web game của tôi!</p>
    </header>

    <div id="game-container">
        <canvas id="gameCanvas" width="400" height="500"></canvas>
    </div>

    <div class="stats">Điểm: <span id="score">0</span></div>

    <div class="instructions">
        Sử dụng <b>Phím Mũi Tên Trái/Phải</b> hoặc <b>Chuột</b> để di chuyển thanh đỡ.
    </div>

    <script>
        const canvas = document.getElementById("gameCanvas");
        const ctx = canvas.getContext("2d");
        const scoreElement = document.getElementById("score");

        let score = 0;
        let basket = { x: 175, y: 450, width: 50, height: 20, speed: 7 };
        let item = { x: Math.random() * 370, y
